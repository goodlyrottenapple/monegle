[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"

# Monad testnet configuration
[rpc_endpoints]
monad_testnet = "https://testnet-rpc.monad.xyz"

[etherscan]
monad_testnet = { key = "${MONAD_ETHERSCAN_KEY}", chain = 10143, url = "https://testnet-explorer.monad.xyz/api" }
